// 1. Defining the nested array
let nums = [ [2, 4], [3, 6], [4, 8] ];

// 2. Logging the whole array to the console
console.log(nums); 

// 3. Accessing specific elements
console.log(nums[0]);    // Outputs: [2, 4]
console.log(nums[1][1]); // Outputs: 6 (The second element of the second array)